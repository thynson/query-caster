{
  "name": "query-caster",
  "version": "0.0.1",
  "description": "Yet another query builder written in Typescript",
  "main": "built/index.js",
  "typings": "built/index.d.ts",
  "scripts": {
    "prepublish": "typings install && tsc",
    "test": "istanbul cover -e .ts  -x \"*.d.ts\" -x \"*.spec.ts\" node_modules/.bin/mocha -- --compilers ts:ts-node/register test/*.ts",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "SQL",
    "builder",
    "Typescript"
  ],
  "author": "LAN Xingcan",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^2.11.12",
    "istanbul": "^1.1.0-alpha.1",
    "mocha": "^3.0.2",
    "source-map-support": "^0.4.2",
    "ts-node": "^1.3.0",
    "typescript": "^2.0.0",
    "typings": "^1.3.2"
  }
}
